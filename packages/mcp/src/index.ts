/**
 * @hypha/mcp — Hypha's MCP server (6 intent-shaped tools + resource templates).
 *
 * Full implementation lands in W7-8 per ~/.claude/plans/v1-b-zippy-wadler.md.
 */

export const MCP_VERSION = '0.1.0-dev';
