/**
 * @hypha/cli — `hypha ingest`, `hypha infer`, `hypha serve`, `hypha publish`, etc.
 *
 * Full implementation lands in W3-4 (ingest, build-adapter) and W11-12 (publish, export-graphiti).
 */

export const CLI_VERSION = '0.1.0-dev';
