{
  "name": "@hypha/store-sqlite",
  "version": "0.1.0-dev",
  "description": "SQLite implementation of Hypha's Store interface (bun:sqlite + sqlite-vec + FTS5).",
  "type": "module",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hypha/core": "workspace:*",
    "sqlite-vec": "^0.1.6"
  }
}
