{
  "name": "@hypha/governance",
  "version": "0.1.0-dev",
  "description": "Hypha governance — Cedar policy engine, capability tokens, biometric DEK unlock, audit log.",
  "type": "module",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hypha/core": "workspace:*"
  }
}
